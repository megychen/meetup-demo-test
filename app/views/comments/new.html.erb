<div class="col-md-4 col-md-offset-4">
  <h2 class="text-center">Add Comment</h2>

  <%= simple_form_for [@meetup, @comment] do |f| %>
    <%= f.input :body %>
    <%= f.submit "Submit", class: "btn btn-primary", :disable_with => "Submitting..." %>
  <% end %>
</div>
